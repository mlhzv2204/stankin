#include <stdio.h>
#include <stdlib.h>

int main (void){
	int a, b, c, d, maxA, maxB, min, max1, max2;
	printf("Vvedite 4 4isla\n");
	scanf("%d %d %d %d",&a ,&b, &c, &d);
	if(a!=b && a!=c){
		if(a!=d && b!=c){
			if(b!=d && c!=d){
				;
				}}}
				else{
                    printf("Hotya by dva chisla odinakovye po znacheniyu, vyhod iz programmy");
                    exit(1);
				}
	printf("Dany chisla:\nA = %d\nB = %d\nC = %d\nD = %d\nVerno li chto naimen'shee iz nih ravno raznosti dvuh naibol'shih po modulyu?\n", a, b, c, d);
	if(a > b)
		maxA = a;
	else
		maxA = b;

	if(c > d)
		maxB = c;
	else
		maxB = d;

	if(maxA > maxB)
        max1 = maxA;
    else
        max1 = maxB;

    if (max1 == a)
        if(b > c)
            if(b > d){
                max2 = b;
                if (c > d)
                    min = d;
                else
                    min = c;
                }
            else{
                max2 = d;
                if (b > c)
                    min = c;
                else
                    min = b;
                }
        else if (c > d){
                max2 = c;
                if (b > d)
                    min = d;
                else
                    min = b;
                }
        else{
                max2 = d;
                if (b > c)
                    min = c;
                else
                    min = b;
                }

    if (max1 == b)
        if(a > c)
            if(a > d){
                max2 = a;
                if (c > d)
                    min = d;
                else
                    min = c;
                }
            else{
                max2 = d;
                if (a > c)
                    min = c;
                else
                    min = a;
                }
        else if (c > d){
                max2 = c;
                if (a > d)
                    min = d;
                else
                    min = a;
                }
        else{
                max2 = d;
                if (a > c)
                    min = c;
                else
                    min = a;
                }
    if (max1 == c)
        if(a > b)
            if(a > d){
                max2 = a;
                if (b > d)
                    min = d;
                else
                    min = b;
                }
            else{
                max2 = d;
                if (a > b)
                    min = b;
                else
                    min = a;
                }
        else if (b > d){
                max2 = b;
                if (a > d)
                    min = d;
                else
                    min = a;
                }
        else{
                max2 = d;
                if (a > b)
                    min = b;
                else
                    min = a;
                }
    if (max1 == d)
        if(a > b)
            if(a > c){
                max2 = a;
                if (b > c)
                    min = c;
                else
                    min = b;
                }
            else{
                max2 = c;
                if (a > b)
                    min = b;
                else
                    min = a;
                }
        else if (b > c){
                max2 = b;
                if (a > c)
                    min = c;
                else
                    min = a;
                }
        else{
                max2 = c;
                if (a > b)
                    min = b;
                else
                    min = a;
                }

printf("max1 = %d\nmax2 = %d\nmin = %d\n", max1, max2, min);
if(abs(max1 - max2) == min)
		printf("Verno\n");
	else
		printf("Neverno\n");

	return 0;
}
