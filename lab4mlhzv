#include <stdio.h>
#include <conio.h>

void main(void){
char c;
FILE *f1;
f1 = fopen("res.txt", "w");
while ((c = getchar()) != EOF){
if(c == '.')
	for(putchar('.'); c== '.';)
	c = getchar();
putchar(c);
fprintf(f1,"%c",c);
}
fclose(f1);
getch();
}
