#include <stdio.h>
#include <conio.h>

int main(void){
	char c;
	FILE *f1;
	f1 = fopen("result_file.txt", "w");
	while ((c = getchar()) != EOF){
		if(c == '.'){
			fprintf(f1,"%c",c);
			while (((c = getchar()) != EOF) && (c == '.'));
		}
		fprintf(f1,"%c",c);
	}
	fclose(f1);
	return 0;
}
